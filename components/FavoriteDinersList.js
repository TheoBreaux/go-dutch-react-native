import { StyleSheet, Text, View, FlatList } from "react-native";

const FavoriteDinersList = ({item}) => {
  return (
    <View>
      <Text>FavoriteDinersList</Text>
    </View>
  )
}

export default FavoriteDinersList

const styles = StyleSheet.create({})