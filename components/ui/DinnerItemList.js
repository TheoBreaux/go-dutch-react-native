import { useState } from "react";
import { StyleSheet, Text, View } from "react-native";

const DinnerItemList = ({}) => {
  return (
    <View>
      <Text>DinnerItemList</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default DinnerItemList;
