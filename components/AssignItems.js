import { StyleSheet, Text, View } from "react-native";
import React from "react";

const AssignItems = () => {
  return (
    <View>
      <Text>AssignItems</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default AssignItems;
